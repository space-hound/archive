<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:Expenses.Styles">

    <!-- STYLE EACH LIST ITEM "TITLE"  -->
    <Style TargetType="TextBlock" x:Key="ListTitle">
        <Setter Property="FontFamily" Value="Consolas"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="FontSize" Value="18"/>
        <Setter Property="Margin" Value="30, 0, 0, 0"/>
    </Style>

    <!-- STYLE EACH LIST ITEM "VALUE"  -->
    <Style TargetType="TextBlock" x:Key="ListValue" BasedOn="{StaticResource ListTitle}">
        <Setter Property="HorizontalAlignment" Value="Right"/>
        <Setter Property="FontWeight" Value="Medium"/>
        <Setter Property="Margin" Value="0, 0, 30, 0"/>
    </Style>

    <!-- STYLE EACH LIST ITEM "ICON"  -->
    <Style TargetType="Ellipse" x:Key="ListIcon">
        <Setter Property="Width" Value="20"/>
        <Setter Property="Height" Value="20"/>
        <Setter Property="Stroke" Value="{StaticResource Black}"/>
        <Setter Property="Margin" Value="20, 0, 0, 0"/>
    </Style>

    <!-- MAKES EACH LIST ITEM STRECH AND MAKE IT FULL WIDTH OF CONTAINER -->
    <Style TargetType="ListViewItem">
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="Padding" Value="0, 5, 0, 5"/>
    </Style>
</ResourceDictionary>